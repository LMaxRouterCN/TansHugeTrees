# 刀T定罪报告 + 刀R手术单 (2026-09-22 03:00)

## 一、判决: DeferredQueue 溢出驱逐 = 树消失凶手
- 本场数据: 28,857 次驱逐 / 20,813 个 unique chunk (35个扫描region=35,840 chunk 的 58%)
- 机制链: region 认领跳过的 chunk -> region 扫完时 wakeOnRegionComplete 批量唤醒 ->
  先摘牌(移出 pendingEmptyChunks)再入队 -> 队列满 -> evictOldest 丢最老非forced ->
  被丢 chunk 已不在等待集 = 永久孤儿: 树数据在 bin 里, start() 永远不会重跑
- V42 注释自供"非forced丢了会被唤醒机制补回"=错误: 唤醒只覆盖等待集成员, 而唤醒动作本身先摘牌
- forced 任务被保护不丢(跨chunk方块载荷), 丢的全是 NORMAL 补种任务

## 二、空间实锤 (region: placing树 vs 驱逐次数)
-1,0: 5152 vs 不在top20 | 0,0: 4464 vs 不在top20 | 1,0: 861 vs 2205
-2,1: 117 vs 1494 | 0,3: 0 vs 1744 | 1,3: 0 vs 816
- 中心4region(22:50最早完成,队列尚空)=66%树量; 外围region(23:03-23:30完成)骤降
- 相邻region密度断崖(5152 vs 117)=max"直角纸片直边"真身: region边界两侧一实一空
- 分钟级双榜: 23:26 = watchdog 25episodes + 驱逐3138 (均第一) ->
  日志税拖慢processTick消费 -> 队列积压 -> 驱逐潮; 日志税是加重因素非根因
- local行列0无尖峰: 扫描行列伪影假设被数据否决

## 三、附带结案
- 32x32 region: 09-07 P0决策(memory 078)已判死(玩家中心动态预扫描,旧region逻辑删除不留回滚),
  现存代码=回档吞掉实现(memory 079) — max记忆正确
- 种子案(刀S)结案: region骨架坐标固定与种子无关, 树微观位置由种子定 = "每个世界很像但不一样"
- E2=18162 中 97% ground=snow_block: 雪原拒绝喜草树种, 过滤层清白
- 5个零候选region: 0,3/1,3=驱逐受害者(2560次驱逐0棵); 2,3/-1,3/-3,-2 疑真无候选(rarity/群系), 待复测分辨

## 四、修复三档 (裁决=max, 今晚未动任何代码/配置)
- A档 一行止血: deferred_queue_max_size 4096 -> 65536
  (任务=轻对象~100B, 64K容量约6.5MB, 零OOM风险), 驱逐率立压到~0, 治标, 下一场测试前生效
- B档 小手术: wake摘牌时机后移(start读到数据/终态才摘牌, 被驱逐自动回等待集)+终态清扫兜底,
  关闭孤儿窗口, 治标+; 若走C档则被整体取代, 不必单独做
- C档 根治 = P0复兴(09-07 max定稿): 逐chunk事件驱动预扫描(getData纯数据计算,
  chunk进管线时同步完成, 零常驻chunk), 删除region认领/批量唤醒/wake风暴,
  DeferredQueue只剩±4邻居FULL延迟一种轻负载, 驱逐失去存在条件; 天然适配任意视距(RD7/RD32同构)
  触及TreeLocation/EventCenter/TreePlacer/Caches四件套+世界切换清场, 估2-4轮+回归协议
- 推荐: A立即生效 + C立案主刀

## 五、已落地
- 刀R预备: 三处裸println补键控(TreePlacer:2176 place()入口/TreePlacer:130 dropped-task/DeferredBlocks:37 add()每100块)
  commit b23b6db, jar tanshugetrees-1.8.0-20260922024807 已部署mods
- 调试仪表全开保持(max指令); 刀Q烟测全绿结案