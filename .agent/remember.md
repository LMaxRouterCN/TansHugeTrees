刀L+刀M落码轮(max执行令: 死参数留+Watchdog搬, 方案全谈定): ①落码前抓到关键细节: Handcode旧块if内的Watchdog.thresholdMs/dumpAllThreadsEnabled两条静态赋值若随启动块一起摘掉→阈值永远卡默认50ms+dump失效→处置=赋值无条件保留在Handcode解析处(先于Core侧start执行,顺序成立) ②风险清零三账: setBlock(flag2)内部getChunkAt对已加载chunk=缓存命中零强载(PendingBlocks按块所在chunk分桶+Load事件驱动,目标chunk必加载) / setUnsaved由Level.setBlock内置(原is_world_gen=false主路径从未手动补过=原版一直自己干) / Core调handcode.Watchdog合规(EventCenter调TreePlacer既存先例,一次性启动非高频) ③刀L改动: GameUtils主线程统一flag=2(is_world_gen条件摘除,参数成死参数), 异步防御分支原样, placeForced flag4→2, resyncChunk三调用点不动(双保险) ④刀M改动: Core加watchdog_enabled字段+json解析+模板键+启动块(默认false), Handcode摘enabled解析与start块(10行→8行, 静态赋值保留+threshold加getOrDefault 50ms兜底) ⑤收尸刀清单(验收绿后): resyncChunk三调用点+is_world_gen死参数+Tile.set异步防御分支+Handcode主config死键watchdog_enabled ⑥验收协议: 关游戏重开→进世界55→站2min+跑图→撞墙测幽灵(应即时可见不用跑出视距)→看树影(顺带治愈项)→json开关效力(现有json缺键=false,想开手动加键) ⑦双保险副效果: 32格内双包(增量+全量)客户端幂等无害 ⑧游戏在跑则部署推迟(javaw+minecraft命令行探测), commit照常