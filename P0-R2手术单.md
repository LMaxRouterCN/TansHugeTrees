# P0-R2 手术单: 树预生成引擎换脑 (region 踩踏 → 玩家中心窗口差分)

> **状态: 立项待批 — 本单为纯设计, 代码零改动. max 签字后按 §5 分刀施工.**
> **基线:** commit 338a54b (刀S落地, build 全绿 43s, 546+/8-)
> **需求原文 (max 2026-09-23):** "删了旧的垃圾预生成, 写个新的, 范围配置和 budgetms 一样表达式, 以玩家位置为中心, 视距外的预生成区域只计算不加载不放置"

## 0. 一句话
拆掉"踩到哪整块扫哪"的编排层, 换成"玩家中心半径窗口差分预计算"; 数据层 (getData/缓存/bin 落盘) 原封不动. 换脑不换心脏.

## 1. 病理回顾 (2026-09-23 判读, 五烂点)
1. **踩踏非预测** — 数据与玩家同步到达, "预生成"语义在现架构中不存在 (迟滞原罪, 档案108 天然后置定性)
2. **洪峰无背压** — 1024-chunk 大块任务与放置任务挤兑共享无界 executor (刀F 拆的是强载, 没治调度)
3. **方形断崖** — region 对齐数据边界, 树连锁到界即断链等待
4. **半途废案底** — 世界54 南侧 25/29KB 实证: 扫描中断 = 整 region 丢失
5. **触发覆盖谜** — 133 vs 4300 chunk 会话级波动未定案

认领本体 (V38 三态原子认领) 健壮 — 病在编排, 不在认领. 手术只换编排.

## 2. 手术边界
**保留 (数据层, 原封不动):** getData 逐 chunk 坐标计算 / cache_write_* 内存缓存 / flushCachesAsync 同步落盘链 (V26) / bin region 文件格式 / region_scan_percent 逐 chunk 抽样门 (现值100) / overlay 进度图标 / TreePlacer.start 放置链 + requeueChunk + wake 唤醒链
**拆除 (编排层):** ChunkEvent.Load 踩踏触发入口 → TreeLocation.run 的 region 整块认领 + 1024 顺序循环
**新建 (新脑):** PlayerTick 防抖观察 + 玩家中心窗口差分 + chunk 粒度背压提交 + region 完成记账回写

## 3. 新引擎机制设计
### 3.1 触发 (PlayerTick, O(1) 防抖)
- END 相位每玩家每 tick 比较所在 chunk 坐标; 不变 → 立即返回 (一次比较, 主线程无感)
- 变化 → 全量窗口差分 (bitset O(1) 测试 ×~4000, <0.1ms)
- 多玩家: 各自差分, 共享计算台账 (窗口并集自然形成)

### 3.2 窗口与半径 (表达式, ExprEngine 复用 — 一个引擎两张表)
- 变量表: **v** = 服务器视距, **q** = 预生成待算积压深度
- 默认式: `v + 8` (视距外 8 圈 chunk)
- 语法/热重载/双回退链与刀S 同一套 (编译期槽位绑定已预留)

### 3.3 差分与台账 (内存认账)
- 计算台账: per-dimension Map<regionKey, BitSet(1024)> — 每 region 128B, 百万 chunk 级探索也不到 MB
- 窗口内未算 chunk → 待算队列
- JVM 重启台账清零 = 重算 (与现状 claims 语义一致; 幂等成本见侦察②)
- clearWorldState (刀K 聚合入口) 追加一行, 跨世界零残留 (世界55 判例防线)

### 3.4 背压 (in-flight 上限, 事件链驱动零轮询)
- 待算队列自持 (不灌共享 executor — 旧洪峰的根)
- 仅当 in-flight < pregen_max_inflight 时经 submitTreeGen 提交
- 任务完成回调 → 计数递减 → 钩住补位下一 chunk (链条层层钩住)

### 3.5 冲刷粒度 (三选一, 待 max 拍)
- a. region 完成时一次冲 — 对齐现状, I/O 最少, 半途废案照旧
- b. 每 chunk 即 append — 抗崩最好, I/O ×1024
- c. **聚合批量 (推荐)** — 每 pregen_flush_batch (默认64) chunk 或 region 完成时冲, 完成计数触发, 零定时器

### 3.6 记账回写 — 判定权免迁移 (本设计核心)
region 的 1024 bit 全满 → 冲刷先行 → `region_scan_claims.put(regionKey, TRUE)`
→ allNeighborRegionsComplete / wakeOnRegionComplete / pendingEmptyChunks / 终态判定 **一行不改全兼容** (claims 语义不变: TRUE = 数据已落盘). V42 不变量 "TRUE 蕴含落盘" 由 3.5 冲刷先行保证. 原判读"真难点②判定权迁移"被此设计消解.

### 3.7 视距外语义映射 (max 三条款对账)
- "只计算": getData 纯计算不加载 (侦察①确认后成立)
- "不加载": 预扫任务不碰 chunk 管线 (078 原案②语义)
- "不放置": 放置仍绑真实 chunk 加载事件 (ChunkEvent.Load → DeferredQueue 原链), 窗口外数据安静躺盘上等玩家

## 4. 配置面 (全部 getOrDefault, 旧配置零变化)
| 键 | 默认 | 语义 |
|---|---|---|
| pregen_mode | region | region=旧链(默认) / player_center=新链; **重启生效不热切** (双链中间态复杂, 边界清晰) |
| pregen_radius_expr | v + 8 | 窗口半径表达式 |
| pregen_max_inflight | 8 | 并发预扫上限 (侦察④校准) |
| pregen_flush_batch | 64 | 冲刷聚合粒度 (选 c 时) |

## 5. 分刀施工序 (刀U 系列起跳, 刀T 已被 32x32 案占用; 每刀独立可验, git 锚点+mode 开关双保险)
- **刀U1 观察者:** PlayerTick 订阅 + 差分 + 台账, 只打日志不接线 (log_tree_location 键控) — 零风险纯增量
- **刀U2 计算接线:** mode=player_center 时差分产物 → 背压提交 → getData → cache_write → 聚合冲刷; mode=region 旧链原样运行
- **刀U3 记账回写:** bitset 满 → 冲刷 → claims TRUE → 唤醒链自动接管 (§3.6)
- **刀U4 试验收:** 新档或世界63重启跑对照 (§7), 默认仍 region
- **刀U5 收尸:** 验收过 → 默认翻 player_center → 踩踏入口退役 (TreeLocation.start/run 判死, 数据层方法保留)

## 6. 施工前侦察清单 (动刀前必做)
1. **getData 读侧审计** — 纯种子计算还是 getChunk 读? 决定"只算不载"是白送还是要动 getData (最大未知; 侧证: 世界63 45821 棵树无 35s 冻结 → 倾向纯计算)
2. **bin 重复写入防护** — 现状重启重扫 + writeBIN(append=true) 为何不翻倍? 幂等机制在哪 — 决定新引擎台账幂等策略
3. ChunkEvent.Load → TreeLocation.start 触发链全文 (拆除边界精确划定)
4. TREE_GEN_EXECUTOR 池线程数现值 (背压上限校准)
5. 1.20.1 服务器视距 API 取数路径 (v 变量来源)

## 7. 验收协议
- **对照组:** max 现 jar 跑图行为 (边玩边干, 天然基线采集已在进行)
- **观测:** 直线飞行新区域, 树出现时机 — 旧=到达后迟滞冒树; 新=窗口边缘提前 v+8 chunk 数据就绪
- **仪表:** log_tree_location 键控打点 (diff/提交/region 完成)
- **回退:** pregen_mode 一键切回 region; git 锚点 338a54b 兜底

## 8. 风险与兜底
- 侦察① 若 getData 需读 chunk → 兜底: 窗口差分只对已 FULL chunk 提交 (退化为"提前一格的踩踏", 仍优于现状, 承诺降级写明)
- 侦察② 幂等未知 → 兜底: 按 recon 结果定台账口径 (重启全重算 / 按 bin 容量认账)
- P0-R 前科对照: 本次强载已拆 (刀F/刀N) + 数据层摸透 (V41/V42) + 分刀共存非全有全无 — 当年三死因全消